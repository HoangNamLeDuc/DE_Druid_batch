Task run automatically by scheduler, 10PM each day. 
Output of the result will be in data/{date}/merged_stock_data.csv
Following these steps below:
1. Download/clone repo
2. cd to Automation folder
3. inside the folder, open terminal, then run 2 command consecutively:
    docker compose build
    docker compose up

